---
name: js-ts-style
description: "JavaScript and TypeScript coding style rules. Use when creating, editing, or working with any JavaScript or TypeScript code or project."
---

# Basics

- follow the repo's existing package manager, formatter, linter, and test runner
- do not guess package versions or APIs. use npm-search when needed
- prefer the repo's existing patterns over generic rewrites

## Finish flow

After a full JS or TS edit pass is done, run the repo's existing checks in the normal project flow before you stop.
