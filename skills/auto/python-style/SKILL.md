---
name: python-style
description: "Python coding style rules. Use when creating, editing, or working with any Python code or Python project."
---

# Basics

- no inline comments
- follow the repo's existing formatter, linter, and test runner
- do not guess package versions or APIs. use pypi when needed
- prefer the repo's existing patterns over generic rewrites

## Finish flow

After a full Python edit pass is done, run the repo's existing checks in the normal project flow before you stop.
