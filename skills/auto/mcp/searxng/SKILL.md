---
name: searxng
description: Use for outside facts after specific tools fail.
---

# SearXNG

Use `web_search` to find outside information.

Use `web_fetch` to read the full page after that.

Use this after the more specific tools first, not before them.
